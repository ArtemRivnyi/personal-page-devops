# Используем легковесный nginx образ для раздачи статики
FROM nginx:alpine

# Копируем наши файлы в папку, которую nginx использует по умолчанию
COPY . /usr/share/nginx/html

# Мы используем дефолтный порт 80, поэтому EXPOSE не обязателен,
# но это хорошая практика.
EXPOSE 80